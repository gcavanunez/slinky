# Implementation Plans

Generated by the improve skill on 2026-07-16. Execute in the order below unless dependencies say otherwise. Each executor must read the selected plan fully, honor its STOP conditions, and update its row when done.

The repository currently has no Git commits, so plans use recorded file checksums instead of a commit-based drift check. Create an initial baseline commit before using isolated-worktree execution.

## Execution Order And Status

| Plan | Title | Priority | Effort | Depends on | Status |
|---|---|---|---|---|---|
| 001 | Consolidate catalog mutation workflows | P1 | M | - | TODO |

Status values: TODO | IN PROGRESS | DONE | BLOCKED (with one-line reason) | REJECTED (with one-line rationale)

## Dependency Notes

- Plan 001 is standalone. It creates the application boundary that later dry-run, scoped-reconciliation, and transactional-filesystem work should extend rather than bypass.

## Findings Considered And Rejected

- A wholesale migration to Stack-style Effect services is rejected for this finding. Slinky needs one shared application module, not service wrappers around every filesystem path and operation.
- Full CLI/TUI feature parity is rejected. The TUI may remain a focused interactive catalog while both entry points share mutation semantics.
