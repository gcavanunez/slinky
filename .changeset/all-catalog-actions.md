---
"@gcavanunez/slinky": minor
---

Add `slinky restore all` to reset every drifting live vendor skill from the catalog baseline, and add positional `slinky adopt all` as an equivalent to `slinky adopt --all` for importing every unindexed global or staged skill.
