---
"@gcavanunez/slinky": patch
---

Publish standalone macOS and Linux binaries for ARM64 and x64. The main npm package now selects the matching platform package and no longer requires Bun at runtime.
