---
"@gcavanunez/slinky": minor
---

Add host-local OpenCode V2 invocation preferences through `slinky autoinvoke` and the TUI's `A` key. Render invocation metadata into global installations, translate portable manual-only flags, and keep generated additions out of source hashes, vendor diffs, and accepted upstream updates.
