---
"@gcavanunez/slinky": minor
---

Drive the fleet from the TUI. On a leader, `S` now runs the fleet sync (this machine, then every follower, each reported as it finishes), and `m` opens the fleet to add, edit, remove, and connection-check followers. Open modals also dim the view behind them.
