---
"@gcavanunez/slinky": minor
---

Add safe `push`, fast-forward-only `pull`, and `sync --pull` workflows for sharing a catalog across machines while preserving each machine's local state. Add `slinky version` as a discoverable equivalent to `slinky --version`.
