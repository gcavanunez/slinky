---
"@gcavanunez/slinky": minor
---

Redesign the TUI: open panes divided by rails with a single foldable catalog tree (headings toggle, fold, and summarise their group), labelled status columns, persistent document zoom, `f` to reveal `SKILL.md` frontmatter, and 27 colour themes chosen with `t` or `slinky config theme`.
