---
"@gcavanunez/slinky": minor
---

Add `slinky skills add` for lock-aware skills.sh installation, surface host skill directories that are not indexed in the manifest, let users index matching skills from the TUI, show project placement and Git visibility on skill rows, and provide top-level `available here` and `all skills` catalog tabs.
